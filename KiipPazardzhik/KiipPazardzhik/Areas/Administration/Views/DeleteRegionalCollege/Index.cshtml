@{
    ViewData["Title"] = "Изтрий регионална колегия";
}

@model DeleteRegionalCollegeBaseModel

<h1 class="mt-4">@ViewData["Title"]</h1>
<hr />
<partial name="_TempDataPartial" />

<form method="post" enctype="multipart/form-data">
    <div class="row">
        <div class="form-group col-lg-12">
            <label asp-for="@this.Model.DeleteRegionalCollegeInputModel.Id"></label>
            <select class="form-control" asp-for="@this.Model.DeleteRegionalCollegeInputModel.Id">
                <option value="" disabled selected>Избери...</option>
                @foreach (var document in this.Model.AllRegionalColleges)
                {
                    <option value="@document.Id">@document.Name</option>
                }
            </select>
            <span asp-validation-for="@this.Model.DeleteRegionalCollegeInputModel.Id" class="text-danger"></span>
        </div>
    </div>
    <div class="row">
        <div class="form-group col-lg-12">
            <button class="btn btn-success" asp-area="Administration" asp-controller="DeleteRegionalCollege" asp-action="DeleteRegionalCollege">
                Изтрий
            </button>
        </div>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/lib/tinymce/tinymce.min.js" type="text/javascript"></script>
    <script src="~/lib/tinymce/visualizer.js" type="text/javascript"></script>
}